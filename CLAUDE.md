# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

基于 Docusaurus 3.10 构建的个人学习笔记博客，使用 React 19，Node >= 20。支持本地中英文搜索，集成 AI 聊天助手。博客（blog）功能已禁用，站点只有两类内容：「理论知识」（内容目录 `docs/`）和「项目实战」（内容目录 `projects/`）。另有 `examples/` 存放配套可运行示例代码，独立依赖树，不参与站点构建。

## 常用命令

```bash
# 前端（仓库根目录）
npm install     # 安装依赖
npm run start   # 启动开发服务器 (localhost:3000)
npm run build   # 生产构建，输出到 build/
npm run serve   # 预览生产构建
npm run clear   # 清除 .docusaurus 缓存（配置或内容异常时先跑这个）

# API 后端（server/ 目录，独立依赖树）
cd server
npm install
npm run dev     # node --watch，监听 :3001
npm start       # 生产启动

# 示例代码（examples/mini-chatbi，独立依赖树）
cd examples/mini-chatbi
npm install
npm run seed              # 生成 SQLite 示例库 mini-chatbi.db
npm run ask "<问题>"      # 跑一次完整问答链路
npm run eval              # 黄金样本评测，输出准确率
npm test                  # vitest run
```

`examples/mini-chatbi` 用内置 `node:sqlite`，需 Node >= 22.13（Node 20 无该模块，22.5–22.12 需 `--experimental-sqlite`）；根目录站点仍只要 Node >= 20。注意其 plan 文档里写的「Node >= 20」与实际不符。

## 架构

### 内容与路由（两个独立的 docs 插件）

站点有两套并列的文档内容，各自有独立的路由和自动生成侧边栏：

- **理论知识**（导航栏 label「理论知识」）— preset-classic 的默认 docs 插件。内容在 `docs/`，路由 `/notes`（注意 `routeBasePath: 'notes'`，不是默认的 `/docs`），侧边栏配置 `sidebars.js`（sidebarId `tutorialSidebar`）。按 `01-fundamentals` … `08-product-engineering` 八个编号目录组织。
- **项目实战**（导航栏 label「项目实战」）— 额外注册的 `@docusaurus/plugin-content-docs` 实例（id `projects`）。内容在 `projects/`，路由 `/projects`，侧边栏配置 `sidebarsProjects.js`。结构是扁平的编号文件（`01-backend-automation.md` … `05-data-viz.md`），只有拆成多篇的系列才建目录（如 `06-chat-bi/`）。

两个侧边栏都用 `autogenerated`，从对应目录结构自动生成，无需手动维护条目。要新增内容分类，在 `docs/` 或 `projects/` 下建文件夹即可。两个板块的首页（`docs/intro.md`、`projects/index.md`）都是手写目录表格，新增内容需手动补进去。

### 计划与设计文档（两个位置，一个会发布）

- `superpowers/plans|specs/` — 早期文档（2026-05/06），在站点内容目录之外，不会发布。
- `docs/superpowers/plans|specs/` — 之后的文档都在这里。**它位于 `docs/` 下，会被 docs 插件收录、发布到 `/notes/superpowers/…` 并进搜索索引。**因此这些文档必须是 MDX 安全的：正文里的 `{...}` 会被当 JS 表达式求值（历史事故：表格里的 `{ok, errors}` 导致构建失败，需改写或转义），相对链接必须指向真实文件，否则 `onBrokenLinks: 'throw'` 直接让构建挂掉。

### 示例代码 `examples/mini-chatbi`

「项目实战 → Chat BI」系列（`projects/06-chat-bi/`）的配套实现：简化版 SuperSonic，TypeScript + ESM，`src/` 下按链路分模块（`knowledge-base` → `parser` → `corrector` → `translator` → `executor`，`pipeline` 编排，`eval` 评测），每个模块配同名 `.test.ts`，用 vitest。LLM 走 OpenAI 兼容接口（`src/llm.ts` 用 `generateObject` + zod 约束输出，惰性读 env），配置在 `examples/mini-chatbi/.env`（`AI_BASE_URL` / `AI_API_KEY` / `AI_MODEL`）。这里用 AI SDK v4 —— 全仓共三份不同的 AI SDK 依赖树：根目录 v6、`server/` v4、本目录 v4。

Docusaurus 只扫描 `docs/`、`projects/`、`src/`、`static/`，所以此目录及其 `node_modules`、`*.db` 产物不影响站点构建。`node:sqlite` 通过 `createRequire` 运行时加载，绕开 vite/vitest 解析器——改这部分时别换回静态 `import`。

### 搜索

`@easyops-cn/docusaurus-search-local`（配置在 `docusaurus.config.js` 的 `themes`）。索引 `/notes` 和 `/projects` 两个路由（`docsRouteBasePath`），不索引 blog。改动内容路由时需同步更新此处的 `docsRouteBasePath`。

### AI 聊天

- 前端组件 `src/components/AIChatWidget/`（悬浮窗，用 `@ai-sdk/react` 的 `useChat`），通过 `src/theme/Root.js` 全局注入。请求发往 `/api/chat`。`Root.js` 只在 `siteConfig.customFields.enableChat !== false` 时挂载该组件，同时会把侧边栏默认设为收起（首次访问写 `docusaurus.sidebar.hidden`）。
- **存在两套后端实现**，对应不同部署方式：
  - `server/`（Express + PM2，用于阿里云自托管）：入口 `server/index.js` 监听 :3001，路由 `server/routes/chat.js` 手动写 SSE 流。用 OpenAI 兼容接口，模型/密钥来自 `server/.env`（`AI_BASE_URL` / `AI_API_KEY` / `AI_MODEL`）。此目录用较旧的 AI SDK v4，与根目录的 v6 不是同一份依赖。
  - `api/chat.js`（Vercel Edge Function）：硬编码通义千问（dashscope），密钥 `DASHSCOPE_API_KEY`，用 `toDataStreamResponse()`。
- 生产环境需由 Nginx / 部署平台把 `/api/chat` 反代到对应后端，前端才能连通。

## 部署（两个目标，推 `main` 会同时跑）

### 阿里云自托管 —— 主站（`.github/workflows/deploy.yml`）

1. `npm run build` 后用 rsync 把 `build/` 同步到 `/var/www/blog`（静态站）。
2. SSH 进服务器重启 `/var/www/blog-api` 的 API 服务（`pm2 reload blog-api`，配置见 `server/ecosystem.config.cjs`）。

**GitHub Secrets:** `SSH_PRIVATE_KEY`、`REMOTE_HOST`、`REMOTE_USER`。

服务器辅助文件：`deploy/nginx.conf`（含 `/api/` → `127.0.0.1:3001` 反代与 Let's Encrypt 配置）、`deploy/setup-server.sh`。

### GitHub Pages 镜像（`.github/workflows/gh-pages.yml`）

以 `DEPLOY_TARGET=pages` 构建并发布到 Pages。`docusaurus.config.js` 顶部的 `isPages` 开关据此切三处：`url`/`baseUrl` 改成 `https://kayla-wang.github.io` + `/Personal-Blog-AI/`，以及 `customFields.enableChat: false` 关掉聊天组件（Pages 纯静态无后端）。

`baseUrl` 的大小写必须和 GitHub 仓库规范名 `Personal-Blog-AI` 完全一致 —— Pages 的路径大小写敏感，写成 `/personal-blog-ai/` 会让镜像站所有资源 404（浏览器控制台报 "We suggest trying baseUrl = /Personal-Blog-AI/"）。注意 `git remote -v` 里是小写，那只是 GitHub 的大小写不敏感重定向，不能作为依据；规范名以 `https://api.github.com/repos/Kayla-wang/personal-blog-ai` 返回的 `name` 字段为准。

**改配置时注意**：任何与 url / baseUrl / 聊天相关的改动都要顺着 `isPages` 两个分支都想一遍，否则镜像站会坏链或出现连不通的聊天窗。本地默认按自托管分支构建（`url` 仍是占位的 `https://your-domain.com`）。

## 添加内容

在 `docs/`（理论知识）或 `projects/`（项目实战）下创建文件夹和 Markdown。用 frontmatter 设置元信息：

```markdown
---
title: 文章标题
tags: [标签1, 标签2]
sidebar_position: 1
---
```

写完记得：把条目补进对应板块首页的目录表格（`docs/intro.md` / `projects/index.md`），并跑一次 `npm run build` —— 坏链和 MDX 求值问题只在构建时暴露（`onBrokenLinks: 'throw'`），dev server 不报。
